# tests/unity/ is vendored upstream source (see its README) — formatters must
# not touch it or it stops being diffable against upstream. gitleaks still scans it.
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
        exclude: ^tests/unity/
      - id: end-of-file-fixer
        exclude: ^tests/unity/

  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v17.0.6
    hooks:
      - id: clang-format
        types_or: [c, c++]
        exclude: ^tests/unity/

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.1
    hooks:
      - id: gitleaks

  - repo: local
    hooks:
      - id: run-tests
        name: Run unit tests
        entry: make test
        language: system
        pass_filenames: false
        always_run: true
      - id: conventional-commit
        name: Conventional commit message
        entry: "sh -c 'head -1 \"$1\" | grep -qE \"^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\\(.+\\))?!?: .+\"' --"
        language: system
        stages: [commit-msg]
        always_run: true
